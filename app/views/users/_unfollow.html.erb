<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
                                 html: {method: :destroy}) do |f| %>
  <%= f.submit 'フォローを外す', class: 'btn follow-btn' %>
<% end %>